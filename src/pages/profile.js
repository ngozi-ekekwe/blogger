import React from 'react';
import DefaultLayout from '../layout/DefaultLayout';

const profile = () => {
  return (
    <DefaultLayout>
      profile
    </DefaultLayout>
  );
};

export default profile;