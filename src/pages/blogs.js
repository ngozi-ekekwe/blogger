import React from 'react';
import DefaultLayout from '../layout/DefaultLayout';

const blogs = () => {
  return (
    <DefaultLayout>
      all blogs
    </DefaultLayout>
  );
};

export default blogs;